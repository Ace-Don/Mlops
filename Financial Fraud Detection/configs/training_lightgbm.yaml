# Fraud Detection — LightGBM Training Config
# Pipeline: fraud_training_pipeline with model_type='lightgbm'

model:
  name: fraud_detection_model

parameters:
  target: "fraud"
  model_type: "lightgbm"
  min_train_accuracy: 0.80
  min_test_accuracy: 0.80
