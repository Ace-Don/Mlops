# environment configuration
settings:
  docker:
    required_integrations:
      - sklearn
      - xgboost
      - lightgbm
      - mlflow
    requirements:
      - pyarrow
      - catboost
      - pandas

# configuration of the Model Control Plane
model:
  name: financial_fraud_classifier
  version: lightgbm
  license: Apache 2.0
  description: A financial fraud classifier using LightGBM
  tags: ["fraud_detection", "classifier", "lightgbm"]

# Configure the pipeline
parameters:
  model_type: "lightgbm"
  target: "fraud"

# Enable caching for steps
steps:
  model_trainer:
    enable_cache: True
  model_evaluator:
    enable_cache: True
